{
  "name": "hoard.js",
  "version": "0.1.0",
  "description": "Blazing fast in-memory caching library for Node.js",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test mocha test",
    "coverage": "istanbul cover _mocha",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": ["cache", "caching", "memory cache", "memory caching"],
  "author": "Saïd Mimouni <sayid.mimouni@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/saidM/hoard.js"
  },
  "xo": {
    "space": true,
    "semicolon": false,
    "envs": [
      "node",
      "mocha"
    ]
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^4.0.1"
  }
}
